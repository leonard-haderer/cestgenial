# 🚀 Démarrage Rapide - Projet CGénial

## Installation en 3 étapes

### 1. Installer les dépendances
```bash
pip install -r requirements.txt
```

### 2. Lancer l'application

**Interface Console :**
```bash
python main.py
```

**Interface Web :**
```bash
python run_web.py
```
Puis ouvrez : **http://localhost:8080**

### 3. Choisir une option dans le menu

## ⚡ Test Rapide (sans menu)

Pour tester rapidement toutes les fonctionnalités :
```bash
python exemple_utilisation.py
```

## 📋 Checklist de Vérification

- [ ] Python 3.7+ installé
- [ ] Dépendances installées (`pip install -r requirements.txt`)
- [ ] Fichiers de données présents dans `data/`
- [ ] Application lancée avec succès (`python main.py` ou `python run_web.py`)

## 🎯 Première Utilisation Recommandée

1. **Option 1** : Afficher les données sources (voir ce qui est chargé)
2. **Option 2** : Lancer l'allocation (utiliser les valeurs par défaut)
   - ⚠ Seules les crises actuelles seront considérées
3. **Option 3** : Créer la carte (visualiser les résultats)
4. **Option 4** : Faire une prédiction (tester le ML)

## ⚠️ Important : Crises Actuelles

L'allocation ne considère que les crises avec `en_cours=True` dans le fichier CSV.
Vérifiez que certaines crises sont marquées comme actuelles pour que l'allocation fonctionne.

## 📚 Documentation Complète

- **README.md** : Documentation complète du projet
- **USAGE.md** : Guide d'utilisation détaillé (si disponible)

## ❓ Problème ?

1. Vérifiez que tous les fichiers sont présents
2. Vérifiez l'installation des dépendances : `pip list`
3. Consultez la section Dépannage du README.md

---

**Bon projet ! 🎓**


